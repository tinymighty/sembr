define(['trackr/collections/plantings'],
function(Places, Place) {

	return describe('Places Collection', function() {
 
    beforeEach(function () {
      /*that.todos = new Todo.Collection();
      that.view = new View({collection: that.todos});
      that.mockData = { title: 'Foo Bar', timestamp: new Date().getTime() };
      $('#sandbox').html(that.view.render().el);*/
    });

    /*it("should load all places", function() {
	    player.play(song);
	    expect(player.currentlyPlayingSong).toEqual(song);

	    //demonstrates use of custom matcher
	    expect(player).toBePlaying(song);
	  });*/

	  /*  // demonstrates use of spies to intercept and test method calls
  it("tells the current song if the user has made it a favorite", function() {
    spyOn(song, 'persistFavoriteStatus');

    player.play(song);
    player.makeFavorite();

    expect(song.persistFavoriteStatus).toHaveBeenCalledWith(true);
  });*/
  
  });

});